# 状态管理模块文档

**文件**: `Software/state.py`

## 1. 模块职责

全局配置管理 + PyQt信号通信

---

## 2. 核心类：State

```python
class State(QObject):
    """系统状态持有者，支持信号通知"""
    
    # 信号定义
    connection_changed = pyqtSignal(bool)
    parameters_changed = pyqtSignal(dict)
```

### 2.1 初始化

```python
def __init__(self):
    super().__init__()
    
    # 线程状态
    self._communication_thread = False
    self.data_processing_thread = False
    self.detection_thread = False
    
    # 统计信息
    self.packet_size = 128
    self.sent_frames = 0
    self.received_frames = 0
    
    # 加载配置
    self._parameters = self._load_parameters()
    
    # 设备信息
    self.device_ip = "127.0.0.1"
    self.device_port = 5000
```

---

## 3. 配置管理

### 3.1 加载配置

```python
def _load_parameters(self):
    """从parameters.json加载配置"""
    config_path = Path(__file__).parent / "parameters.json"
    
    if config_path.exists():
        return json.loads(config_path.read_text(encoding="utf-8-sig"))
    else:
        return self._get_default_parameters()
```

### 3.2 保存配置

```python
def save_parameters(self):
    """保存配置到parameters.json"""
    config_path = Path(__file__).parent / "parameters.json"
    config_path.write_text(
        json.dumps(self._parameters, indent=2, ensure_ascii=False),
        encoding="utf-8"
    )
```

### 3.3 读写参数

```python
def get_parameter(self, group: str, name: str, default=None):
    """获取参数值"""
    return self._parameters.get(group, {}).get(name, default)

def set_parameter(self, group: str, name: str, value):
    """设置参数并保存，触发信号"""
    if group not in self._parameters:
        self._parameters[group] = {}
    
    old_value = self._parameters[group].get(name)
    self._parameters[group][name] = value
    
    # 保存到文件
    self.save_parameters()
    
    # 发射信号
    self.parameters_changed.emit({
        "group": group,
        "name": name,
        "value": value,
        "old_value": old_value
    })
```

---

## 4. 属性访问器

### 4.1 接收机参数

```python
@property
def fft_length(self):
    return self.get_parameter("Receiver", "FFT_Length", 512)

@property
def decimation_factor(self):
    return self.get_parameter("Receiver", "Decimation_factor", 100)

@property
def center_frequency(self):
    return self.get_parameter("Receiver", "Centre_frequency(MHz)", 2400.0)

@property
def span(self):
    return self.get_parameter("Receiver", "SPAN(MHz)", 100.0)

@property
def sample_rate(self):
    return 2e9  # 固定2GHz
```

### 4.2 检测参数

```python
@property
def conf_threshold(self):
    return self.get_parameter("Detection", "conf_threshold", 0.25)

@property
def iou_threshold(self):
    return self.get_parameter("Detection", "iou_threshold", 0.45)

@property
def image_size(self):
    return self.get_parameter("Detection", "image_size", 640)
```

### 4.3 扫描参数

```python
@property
def enable_scanning(self):
    return self.get_parameter("Scanner", "enable_scanning", False)

@property
def scan_bandwidth_mhz(self):
    return self.get_parameter("Scanner", "scan_bandwidth_mhz", 100)

@property
def overlap_ratio(self):
    return self.get_parameter("Scanner", "overlap_ratio", 0.5)

@property
def control_lost_threshold(self):
    return self.get_parameter("Scanner", "control_lost_threshold", 5)

@property
def start_frequency_mhz(self):
    return self.get_parameter("Scanner", "start_frequency_mhz", 1000)
```

### 4.4 数据处理参数

```python
@property
def waterfall_height(self):
    return self.get_parameter("Data_Process", "waterfall_height", self.fft_length)

@property
def enable_noise_filter(self):
    return self.get_parameter("Data_Process", "enable_noise_filter", False)

@property
def noise_filter_mode(self):
    return self.get_parameter("Data_Process", "noise_filter_mode", "subtraction")

@property
def noise_alpha(self):
    return self.get_parameter("Data_Process", "noise_alpha", 0.05)
```

---

## 5. 信号机制

### 5.1 连接状态信号

```python
@property
def communication_thread(self):
    return self._communication_thread

@communication_thread.setter
def communication_thread(self, value: bool):
    """设置连接状态，自动发射信号"""
    if self._communication_thread != value:
        self._communication_thread = value
        self.connection_changed.emit(value)
        logger.info(f"连接状态变更: {value}")
```

**使用示例**：
```python
# UI监听
state.connection_changed.connect(self.on_connection_change)

# Communication设置
state.communication_thread = True  # 自动触发信号 → UI更新
```

### 5.2 参数变更信号

```python
# 自动由set_parameter()触发
self.parameters_changed.emit({
    "group": "Detection",
    "name": "conf_threshold",
    "value": 0.3,
    "old_value": 0.25
})
```

---

## 6. 配置文件结构

### parameters.json

```json
{
  "Receiver": {
    "FFT_Length": 512,
    "Decimation_factor": 100,
    "Centre_frequency(MHz)": 2400.0,
    "SPAN(MHz)": 100.0,
    "Channel_count": 20
  },
  "Detection": {
    "conf_threshold": 0.25,
    "iou_threshold": 0.45,
    "image_size": 640
  },
  "Scanner": {
    "enable_scanning": true,
    "scan_bandwidth_mhz": 100,
    "overlap_ratio": 0.5,
    "control_lost_threshold": 5,
    "start_frequency_mhz": 1000
  },
  "Data_Process": {
    "waterfall_height": 512,
    "enable_noise_filter": false,
    "noise_filter_mode": "subtraction",
    "noise_alpha": 0.05
  },
  "UI_Spectrum": {
    "spectrum_left_freq(MHz)": 0.0,
    "spectrum_right_freq(MHz)": 200.0
  },
  "UI_Waterfall": {
    "waterfall_height": 512
  }
}
```

---

## 7. 使用示例

### 7.1 基本使用

```python
# 创建state
state = State()

# 读取参数
fft_len = state.fft_length
conf = state.conf_threshold

# 修改参数（自动保存+触发信号）
state.set_parameter("Detection", "conf_threshold", 0.3)

# 监听信号
state.parameters_changed.connect(lambda data: 
    print(f"参数变更: {data['group']}.{data['name']} = {data['value']}")
)
```

### 7.2 UI集成

```python
class MainWindow(QWidget):
    def __init__(self, state):
        self.state = state
        
        # 监听连接状态
        state.connection_changed.connect(self.update_connection_status)
        
        # 监听参数变更
        state.parameters_changed.connect(self.on_parameter_changed)
    
    def update_connection_status(self, connected: bool):
        if connected:
            self.status_label.setText("✓ 已连接")
        else:
            self.status_label.setText("✗ 未连接")
    
    def on_parameter_changed(self, data: dict):
        # 更新UI显示
        pass
```

---

## 8. 设计模式

### 8.1 观察者模式

```
State (被观察者)
  │
  ├─► connection_changed ────► UI组件1
  │                        └──► UI组件2
  │
  └─► parameters_changed ────► ConfigInterface
                           └──► DataProcessor
```

### 8.2 单例模式（推荐）

```python
# main.py
state = State()  # 全局唯一实例

# 传递给各模块
comm = Communication(state, queue)
processor = DataProcessor(state)
detector = DroneDetector(state, processor)
ui = MainWindow(state)
```

---

## 9. 线程安全

- **PyQt信号**: 自动线程安全，跨线程传递
- **属性访问**: 读操作无需锁（不可变数据）
- **配置修改**: 主线程调用`set_parameter()`

---

## 10. 扩展方法

### 10.1 添加新参数组

```json
{
  "NewModule": {
    "param1": 100,
    "param2": "value"
  }
}
```

```python
# 添加属性访问器
@property
def new_param(self):
    return self.get_parameter("NewModule", "param1", 100)
```

### 10.2 添加新信号

```python
class State(QObject):
    new_signal = pyqtSignal(str)
    
    def trigger_event(self, msg):
        self.new_signal.emit(msg)
```

---

## 总结

State模块通过以下机制实现全局配置管理：

1. **JSON持久化**: 自动加载/保存配置文件
2. **属性访问**: 简洁的@property接口
3. **信号通知**: PyQt信号自动更新UI
4. **线程安全**: 信号机制保证跨线程通信
5. **可扩展**: 易于添加新参数和信号
