# 算法层（Algorithm Layer）模块文档

## 1. 模块概述

算法层是无人机检测系统的核心智能模块，负责使用深度学习目标检测算法（YOLO）对时频图进行分析，识别无人机信号特征并标注检测结果。

**文件位置**：`Software/algorithms.py`

**主要职责**：
- 从数据处理层获取RGB时频图
- 使用YOLO模型进行实时目标检测
- 在图像上绘制检测框和标签
- 维护带检测结果的图像供UI显示
- 提供检测统计信息和配置接口

**技术特点**：
- **深度学习**：基于YOLOv8的目标检测
- **实时性**：独立检测线程，不阻塞数据采集
- **可视化**：自动在检测图像上绘制结果
- **线程安全**：完善的锁机制保护共享数据

---

## 2. YOLO算法原理

### 2.1 YOLO简介

**YOLO（You Only Look Once）** 是一种单阶段目标检测算法，由Joseph Redmon等人于2015年提出。其核心思想是将目标检测问题转化为回归问题，一次性预测所有边界框和类别概率。

**主要优势**：
- ⚡ **速度快**：单次前向传播即可完成检测，实时性强
- 🎯 **全局信息**：考虑整张图像的上下文信息
- 🔧 **端到端**：统一的网络结构，易于训练和优化
- 📦 **通用性强**：可用于各种目标检测任务

### 2.2 YOLO工作原理

#### 2.2.1 核心思想

YOLO将输入图像划分为 **S × S 网格（Grid）**，每个网格单元负责预测：
1. **边界框（Bounding Box）**：目标的位置和大小
2. **置信度（Confidence）**：该框包含目标的概率
3. **类别概率（Class Probability）**：目标属于各个类别的概率

```
输入图像 (1024×1024)
    ↓
划分为 S×S 网格 (例如 13×13)
    ↓
每个网格预测 B 个边界框
    ↓
每个框预测：[x, y, w, h, confidence, class_probs]
    ↓
非极大值抑制（NMS）
    ↓
最终检测结果
```

#### 2.2.2 网络结构

YOLO网络主要由三部分组成：

**1. Backbone（主干网络）**
- 作用：提取图像特征
- 结构：卷积神经网络（如CSPDarknet、EfficientNet）
- 输出：多尺度特征图

**2. Neck（颈部网络）**
- 作用：融合不同尺度特征
- 结构：特征金字塔网络（FPN）+ 路径聚合网络（PAN）
- 目的：增强多尺度目标检测能力

**3. Head（检测头）**
- 作用：预测边界框和类别
- 结构：多个卷积层 + 全连接层
- 输出：检测结果张量

```
输入图像 (H×W×3)
    ↓
┌─────────────────┐
│   Backbone      │  提取特征
│  (CSPDarknet)   │  → 多尺度特征图
└────────┬────────┘
         ↓
┌─────────────────┐
│     Neck        │  融合特征
│   (FPN+PAN)     │  → 增强表征
└────────┬────────┘
         ↓
┌─────────────────┐
│     Head        │  预测检测结果
│  (Detection)    │  → [x,y,w,h,conf,cls]
└─────────────────┘
```

#### 2.2.3 检测过程详解

**步骤1: 网格划分**
```python
# 将图像划分为 S×S 网格
grid_size = 13  # 例如
image_size = 1024
cell_size = image_size / grid_size  # 每个网格单元的大小
```

**步骤2: 边界框预测**

每个网格单元预测 B 个边界框（通常 B=3），每个框包含5个值：
- `(x, y)`: 框中心相对于网格单元左上角的偏移（归一化到0-1）
- `(w, h)`: 框的宽高相对于整张图像的比例
- `confidence`: 置信度 = P(Object) × IoU

```python
# 边界框格式
bbox = [
    x,           # 中心x坐标（相对网格）
    y,           # 中心y坐标（相对网格）
    w,           # 宽度（相对图像）
    h,           # 高度（相对图像）
    confidence   # 置信度
]
```

**步骤3: 类别预测**

每个网格单元预测 C 个类别的概率分布：
```python
class_probs = [P(class_1|Object), P(class_2|Object), ..., P(class_C|Object)]
```

**步骤4: 最终预测**

网络输出张量形状：`(S, S, B × (5 + C))`
- S: 网格大小
- B: 每个网格的边界框数量
- 5: 边界框参数 (x, y, w, h, conf)
- C: 类别数量

**步骤5: 非极大值抑制（NMS）**

用于去除重复检测框：
```python
1. 按置信度排序所有检测框
2. 选择置信度最高的框作为输出
3. 计算该框与其他框的IoU
4. 删除IoU > 阈值的框（重复检测）
5. 重复2-4，直到处理完所有框
```

#### 2.2.4 损失函数

YOLO使用多任务损失函数，包含三部分：

**1. 定位损失（Localization Loss）**

$$
L_loc = λ_coord × Σ [1_obj × (x_pred - x_true)² + (y_pred - y_true)²]
      + λ_coord × Σ [1_obj × (√w_pred - √w_true)² + (√h_pred - √h_true)²]
$$

- 惩罚边界框中心和尺寸的预测误差
- 使用平方根降低大框的惩罚

**2. 置信度损失（Confidence Loss）**
$$
L_conf = Σ [1_obj × (C_pred - C_true)²] 
       + λ_noobj × Σ [1_noobj × (C_pred - 0)²]
$$
- 有目标时：预测置信度应接近IoU
- 无目标时：预测置信度应接近0

**3. 分类损失（Classification Loss）**
$$
L_cls = Σ [1_obj × Σ_c (p_pred(c) - p_true(c))²]
$$
- 惩罚类别预测误差（交叉熵或均方误差）

**总损失**：
$$
L_total = L_loc + L_conf + L_cls
$$

### 2.3 YOLOv8 改进

本项目使用的 **YOLOv8** 是YOLO系列的最新版本（2023年发布），主要改进包括：

1. **Anchor-Free设计**：
   - 不再使用预定义锚框
   - 直接预测目标中心点和尺寸
   - 简化训练过程，提高泛化能力

2. **新的主干网络**：
   - C2f模块：融合CSPNet和ELAN设计
   - 更好的特征提取能力

3. **解耦检测头**：
   - 分类和定位任务使用独立的卷积层
   - 提高预测精度

4. **Task-Aligned Assigner**：
   - 智能分配正负样本
   - 提高训练效率

5. **损失函数优化**：
   - DFL（Distribution Focal Loss）用于定位
   - BCE（Binary Cross Entropy）用于分类
   - CIoU用于边界框回归

### 2.4 在本项目中的应用

#### 应用场景

本系统将YOLO应用于 **射频时频图无人机检测**：
- **输入**：1024×1024 RGB时频图（瀑布图）
- **输出**：无人机信号区域的边界框
- **类别**：drone（无人机信号）

#### 检测流程

```
时频图采集
    ↓
数据处理层生成RGB图像
    ↓
算法层获取图像
    ↓
YOLO前向推理
    ↓
后处理（NMS）
    ↓
绘制检测框
    ↓
UI显示结果
```

#### 特殊考虑

1. **输入格式**：
   - 时频图具有时间-频率二维特性
   - 需要将dB值映射为RGB可视化
   - 使用Jet色图增强视觉效果

2. **训练数据**：
   - 标注无人机信号在时频图上的位置
   - 数据增强：旋转、缩放、噪声添加
   - 多种无人机型号、距离、环境

3. **性能优化**：
   - 使用轻量级模型（如YOLOv8n）提高速度
   - GPU加速（如可用）
   - 异步检测线程避免阻塞数据流

---

## 3. 类设计

### 3.1 DroneDetector类

```python
class DroneDetector:
    """无人机检测算法类 - 使用YOLO进行目标检测"""
    
    def __init__(self, state, data_processor, model_path="best.pt"):
        """
        初始化检测器
        
        Args:
            state: 系统状态对象
            data_processor: 数据处理器对象
            model_path: YOLO模型路径（.pt文件）
        """
```

### 3.2 核心属性

```python
# 依赖对象
self.state: State                       # 系统状态管理器
self.data_processor: DataProcessor      # 数据处理层

# YOLO模型
self.model: YOLO                        # YOLOv8模型实例
self.model_path: str                    # 模型文件路径
self.algorithm_path: Path               # 算法层目录路径

# 线程管理
self.detect_thread: Thread              # 检测线程
self.detection_lock: threading.Lock     # 保护检测结果

# 检测结果
self.detection_image: np.ndarray        # 带检测框的RGB图像 (H×W×3)
self.detection_results: list            # 检测框信息列表
self.detection_count: int               # 检测次数计数器
self.last_detection_time: float         # 上次检测时间戳

# 统计信息
self.total_detections: int              # 总检测次数
self.total_objects: int                 # 总检测目标数
self.fps: float                         # 检测帧率

# 检测参数
self.conf_threshold: float = 0.25       # 置信度阈值
self.iou_threshold: float = 0.45        # NMS IoU阈值
```

---

## 4. 核心方法

### 4.1 线程管理

#### start_detection()

```python
def start_detection(self):
    """启动检测线程
    
    功能：
    1. 检查模型是否加载
    2. 检查线程是否已运行
    3. 设置状态标志
    4. 创建并启动守护线程
    5. 记录日志
    
    线程特性：
    - daemon=True: 主线程退出时自动退出
    - target=_detection_loop: 线程入口函数
    """
```

**实现细节**：
```python
def start_detection(self):
    if self.model is None:
        logging.error("YOLO模型未加载，无法启动检测")
        return

    if not self.detect_thread or not self.detect_thread.is_alive():
        self.state.detection_thread = True
        self.detect_thread = threading.Thread(
            target=self._detection_loop, 
            daemon=True
        )
        self.detect_thread.start()
        logging.info("✓ 检测线程已启动")
```

#### stop_detection()

```python
def stop_detection(self):
    """停止检测线程
    
    功能：
    1. 设置停止标志
    2. 等待线程退出（超时3秒）
    3. 记录日志
    
    安全退出：
    - 通过标志位控制，不强制终止
    - 给线程足够时间完成当前操作
    - 超时后自动返回（daemon线程会被终止）
    """
```

### 4.2 检测主循环

#### _detection_loop()

```python
def _detection_loop(self):
    """检测主循环（运行在独立线程）
    
    循环流程：
    1. 从数据处理层获取最新RGB图像
    2. 使用YOLO模型进行检测
    3. 解析检测结果
    4. 在图像上绘制检测框和标签
    5. 更新检测结果（线程安全）
    6. 计算FPS
    7. 立即进行下一次检测（无延迟）
    
    异常处理：
    - 捕获所有异常并记录日志
    - 异常时延迟100ms后继续
    - 不会因为单次异常而终止线程
    """
```

**详细实现**：
```python
def _detection_loop(self):
    logging.info("检测循环开始运行...")
    
    while self.state.detection_thread:
        try:
            start_time = time.time()
            
            # 步骤1: 获取输入图像
            input_image = self.data_processor.get_waterfall_image()
            
            if input_image is None or input_image.size == 0:
                logging.debug("未获取到有效图像，跳过此次检测")
                time.sleep(0.01)
                continue
            
            # 验证图像格式
            if len(input_image.shape) != 3 or input_image.shape[2] != 3:
                logging.warning(f"图像格式不正确: {input_image.shape}")
                time.sleep(0.01)
                continue
            
            # 步骤2: YOLO检测
            results = self.model(
                input_image,
                conf=self.conf_threshold,      # 置信度阈值
                iou=self.iou_threshold,        # NMS IoU阈值
                verbose=False,                 # 关闭详细输出
            )
            
            # 步骤3: 处理检测结果
            detection_info = []
            annotated_image = input_image.copy()  # 复制图像用于绘制
            
            if len(results) > 0:
                result = results[0]
                boxes = result.boxes
                
                if boxes is not None and len(boxes) > 0:
                    for box in boxes:
                        # 提取框信息
                        x1, y1, x2, y2 = box.xyxy[0].cpu().numpy()
                        conf = float(box.conf[0].cpu().numpy())
                        cls = int(box.cls[0].cpu().numpy())
                        class_name = self.model.names[cls]
                        
                        # 保存检测信息
                        detection_info.append({
                            "bbox": [int(x1), int(y1), int(x2), int(y2)],
                            "confidence": conf,
                            "class_id": cls,
                            "class_name": class_name,
                        })
                        
                        # 绘制检测框
                        cv2.rectangle(
                            annotated_image,
                            (int(x1), int(y1)),
                            (int(x2), int(y2)),
                            (0, 255, 0),  # 绿色框
                            2,
                        )
                        
                        # 绘制标签（带背景）
                        label = f"{class_name} {conf:.2f}"
                        font = cv2.FONT_HERSHEY_SIMPLEX
                        font_scale = 0.5
                        thickness = 1
                        
                        # 计算文本大小
                        (text_width, text_height), baseline = cv2.getTextSize(
                            label, font, font_scale, thickness
                        )
                        
                        # 绘制文本背景（绿色）
                        cv2.rectangle(
                            annotated_image,
                            (int(x1), int(y1) - text_height - 10),
                            (int(x1) + text_width, int(y1)),
                            (0, 255, 0),
                            -1,  # 填充
                        )
                        
                        # 绘制文本（黑色）
                        cv2.putText(
                            annotated_image,
                            label,
                            (int(x1), int(y1) - 5),
                            font,
                            font_scale,
                            (0, 0, 0),
                            thickness,
                        )
                    
                    self.total_objects += len(boxes)
            
            # 步骤4: 更新检测结果（线程安全）
            with self.detection_lock:
                self.detection_image = annotated_image
                self.detection_results = detection_info
                self.detection_count += 1
                self.last_detection_time = time.time()
                self.total_detections += 1
            
            # 计算FPS
            elapsed = time.time() - start_time
            self.fps = 1.0 / elapsed if elapsed > 0 else 0
            
        except Exception as e:
            logging.error(f"检测异常: {e}", exc_info=True)
            time.sleep(0.1)
    
    logging.info("检测循环已退出")
```

### 4.3 对外接口

#### get_detection_image()

```python
def get_detection_image(self):
    """获取带检测框的图像（线程安全）
    
    Returns:
        np.ndarray or None: shape=(height, width, 3), dtype=uint8
        如果没有检测结果，返回None
    
    线程安全：
    - 使用detection_lock保护
    - 返回图像副本，避免外部修改
    """
    with self.detection_lock:
        return (
            self.detection_image.copy()
            if self.detection_image is not None
            else None
        )
```

#### get_detection_results()

```python
def get_detection_results(self):
    """获取检测结果信息（线程安全）
    
    Returns:
        list: 检测结果列表
        [
            {
                "bbox": [x1, y1, x2, y2],
                "confidence": 0.95,
                "class_id": 0,
                "class_name": "drone"
            },
            ...
        ]
    
    用途：
    - UI显示检测框数量
    - 导出检测结果
    - 统计分析
    """
    with self.detection_lock:
        return self.detection_results.copy()
```

#### get_detection_stats()

```python
def get_detection_stats(self):
    """获取检测统计信息（线程安全）
    
    Returns:
        dict: {
            "detection_count": int,      # 当前检测次数
            "total_detections": int,     # 总检测次数
            "total_objects": int,        # 总检测目标数
            "last_detection_time": float,# 上次检测时间戳
            "current_objects": int,      # 当前帧目标数
            "fps": float                 # 检测帧率
        }
    
    用途：
    - UI显示统计信息
    - 性能监控
    - 系统调试
    """
    with self.detection_lock:
        return {
            "detection_count": self.detection_count,
            "total_detections": self.total_detections,
            "total_objects": self.total_objects,
            "last_detection_time": self.last_detection_time,
            "current_objects": len(self.detection_results),
            "fps": self.fps,
        }
```

### 4.4 配置接口

#### set_confidence_threshold()

```python
def set_confidence_threshold(self, conf):
    """设置置信度阈值
    
    Args:
        conf: float, 0-1之间
        值越高，检测越严格（减少误报，可能漏检）
        值越低，检测越宽松（增加误报，减少漏检）
    
    推荐值：
    - 0.25-0.35: 宽松检测
    - 0.40-0.50: 平衡检测
    - 0.60+: 严格检测
    """
    self.conf_threshold = max(0.0, min(1.0, conf))
    logging.info(f"置信度阈值已设置为: {self.conf_threshold}")
```

#### set_iou_threshold()

```python
def set_iou_threshold(self, iou):
    """设置NMS IoU阈值
    
    Args:
        iou: float, 0-1之间
        值越高，保留更多重叠框
        值越低，抑制更多重叠框
    
    推荐值：
    - 0.40-0.45: 标准抑制
    - 0.50-0.60: 宽松抑制
    """
    self.iou_threshold = max(0.0, min(1.0, iou))
    logging.info(f"IoU阈值已设置为: {self.iou_threshold}")
```

#### load_model()

```python
def load_model(self, model_path):
    """重新加载模型
    
    Args:
        model_path: str, 模型文件路径（.pt格式）
    
    Returns:
        bool: 加载成功返回True，失败返回False
    
    用途：
    - 切换不同训练的模型
    - 更新模型版本
    - 热加载新模型
    """
    try:
        logging.info(f"正在加载新模型: {model_path}")
        self.model = YOLO(model_path)
        self.model_path = model_path
        logging.info("✓ 模型加载成功")
        return True
    except Exception as e:
        logging.error(f"模型加载失败: {e}")
        return False
```

---

## 5. 数据流程

### 5.1 检测数据流

```
┌─────────────────────────────────────────────────────────────┐
│                     数据处理层                               │
│  waterfall_image (1024×1024×3, RGB, uint8)                 │
└────────────────┬────────────────────────────────────────────┘
                 │ get_waterfall_image()
                 ▼
┌─────────────────────────────────────────────────────────────┐
│                     算法层检测线程                           │
│  1. 获取图像                                                 │
│  2. YOLO推理                                                │
│  3. 解析结果                                                │
│  4. 绘制检测框                                              │
│  5. 更新detection_image                                    │
└────────────────┬────────────────────────────────────────────┘
                 │ get_detection_image()
                 ▼
┌─────────────────────────────────────────────────────────────┐
│                     UI层显示线程                             │
│  1. 获取带框图像                                            │
│  2. 降采样（可选）                                          │
│  3. 转换为QPixmap                                           │
│  4. 显示在界面                                              │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 时序图

```
时间 →

数据处理层:  [更新RGB] [更新RGB] [更新RGB] [更新RGB] ...
              ↓         ↓         ↓         ↓
算法层:      [检测]    [检测]    [检测]    [检测]    ...
              ↓         ↓         ↓         ↓
              [绘框]    [绘框]    [绘框]    [绘框]    ...
              ↓         ↓         ↓         ↓
UI层:        [显示]    [显示]    [显示]    [显示]    ...

说明：
- 数据处理层：~10ms更新一次RGB图像
- 算法层：尽快完成检测（取决于YOLO速度）
- UI层：25ms定时器更新显示
- 三层异步运行，互不阻塞
```

---

## 6. 性能优化

### 6.1 模型选择

| 模型 | 大小 | 速度 | 精度 | 推荐场景 |
|------|------|------|------|----------|
| YOLOv8n | 6.2MB | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | 实时检测 |
| YOLOv8s | 22MB | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 平衡 |
| YOLOv8m | 52MB | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 高精度 |

**当前使用**：自定义训练的best.pt模型

### 6.2 推理加速

1. **GPU加速**：
```python
# YOLO自动检测CUDA
model = YOLO("best.pt")  # 自动使用GPU（如可用）
```

2. **批量推理**：
```python
# 暂未实现，未来可优化
results = model([img1, img2, img3])  # 批量处理
```

3. **模型量化**：
```python
# 导出INT8量化模型（未来优化）
model.export(format="onnx", int8=True)
```

### 6.3 线程优化

- ✅ 独立检测线程，不阻塞数据采集
- ✅ 异步处理，UI流畅
- ✅ 无延迟循环，最大化检测速度

---

## 7. 使用示例

### 7.1 基本使用

```python
from algorithms import DroneDetector
from state import State
from data_process import DataProcessor

# 创建对象
state = State()
data_processor = DataProcessor(state)
detector = DroneDetector(state, data_processor, model_path="best.pt")

# 启动检测
detector.start_detection()

# 获取结果
while True:
    # 获取带检测框的图像
    img = detector.get_detection_image()
    if img is not None:
        cv2.imshow("Detection", img)
        cv2.waitKey(1)
    
    # 获取统计信息
    stats = detector.get_detection_stats()
    print(f"FPS: {stats['fps']:.2f}, Objects: {stats['current_objects']}")

# 停止检测
detector.stop_detection()
```

### 7.2 配置参数

```python
# 调整置信度阈值
detector.set_confidence_threshold(0.4)

# 调整NMS阈值
detector.set_iou_threshold(0.5)

# 切换模型
detector.load_model("yolov8n.pt")
```

---

## 8. 常见问题

### Q1: 检测速度慢怎么办？

**A**: 
1. 使用更轻量的模型（YOLOv8n）
2. 降低输入图像分辨率
3. 使用GPU加速
4. 减少检测频率（跳帧）

### Q2: 检测不准确怎么办？

**A**:
1. 调低置信度阈值（增加召回率）
2. 使用更大的模型（YOLOv8m）
3. 收集更多训练数据重新训练
4. 数据增强

### Q3: 误报太多怎么办？

**A**:
1. 提高置信度阈值
2. 调整NMS IoU阈值
3. 后处理过滤（如大小、位置）

### Q4: 如何训练自定义模型？

**A**:
```python
from ultralytics import YOLO

# 加载预训练模型
model = YOLO("yolov8n.pt")

# 训练
model.train(
    data="drone_dataset.yaml",  # 数据集配置
    epochs=100,
    imgsz=1024,
    batch=16,
    name="drone_detection"
)

# 使用训练好的模型
detector = DroneDetector(state, data_processor, 
                        model_path="runs/detect/drone_detection/weights/best.pt")
```

---

## 9. 扩展方向

### 9.1 多模型融合

```python
# 使用多个模型投票
models = [YOLO("model1.pt"), YOLO("model2.pt")]
results = [model(img) for model in models]
# 融合结果
```

### 9.2 目标追踪

```python
# 集成SORT/DeepSORT追踪算法
from deep_sort import DeepSORT
tracker = DeepSORT()
tracks = tracker.update(detections)
```

### 9.3 异常检测

```python
# 检测异常信号模式
if len(detection_results) > threshold:
    trigger_alarm()
```

---

## 10. 参考资料

1. **YOLO论文**：
   - [You Only Look Once: Unified, Real-Time Object Detection (2015)](https://arxiv.org/abs/1506.02640)
   - [YOLOv8 Documentation](https://docs.ultralytics.com/)

2. **目标检测综述**：
   - [Object Detection in 20 Years: A Survey](https://arxiv.org/abs/1905.05055)

3. **射频检测应用**：
   - RF Signal Detection using Deep Learning
   - Spectrum Monitoring with AI

4. **Ultralytics YOLOv8**：
   - GitHub: https://github.com/ultralytics/ultralytics
   - Docs: https://docs.ultralytics.com/

---

**文档版本**: 1.0  
**最后更新**: 2025-01-11  
**作者**: 无人机检测系统开发团队
